# Unity Input 鼠标与键盘输入详解

## 一、Input系统基础概念

### 1.1 Unity输入系统概述

Unity的**Input类**是处理用户输入的核心工具，它提供了统一的接口来检测和响应来自鼠标、键盘、手柄等多种输入设备的操作。通过Input类，开发者可以轻松实现角色控制、菜单交互、摄像机操作等游戏功能。

> 💡 **核心理解**：  
> Input系统将物理设备的原始信号转换为**逻辑化的输入事件**，让开发者无需关心底层硬件细节，专注于游戏逻辑实现[ty-reference](1)。

---

### 1.2 输入检测时机

所有输入检测都应该在**Update()方法**中进行，因为：
- Update每帧执行一次，确保不会错过输入事件
- Input类的方法返回的是**当前帧**的输入状态
- 物理相关的输入应放在FixedUpdate中（较少见）

```csharp
void Update()
{
    // 正确：在Update中检测输入
    if (Input.GetKeyDown(KeyCode.Space))
    {
        Jump();
    }
}

void FixedUpdate()
{
    // 错误：一般不在FixedUpdate中检测瞬时输入
    // 但可以在这里处理持续输入的物理效果
}
```

---

## 二、键盘输入详解

### 2.1 键盘输入检测方法

Unity提供了三种键盘输入检测方法，分别对应不同的输入状态：

| 方法 | 触发时机 | 返回值 | 典型用途 |
|------|----------|--------|----------|
| **GetKeyDown()** | 按键**按下瞬间** | bool | 跳跃、射击、菜单确认 |
| **GetKey()** | 按键**持续按住** | bool | 移动、加速、持续攻击 |
| **GetKeyUp()** | 按键**释放瞬间** | bool | 停止动作、释放技能 |

```csharp
void Update()
{
    // 按下瞬间触发（只执行一次）
    if (Input.GetKeyDown(KeyCode.Space))
    {
        Debug.Log("空格键被按下");
        Jump();
    }
    
    // 持续按住触发（每帧执行）
    if (Input.GetKey(KeyCode.W))
    {
        Debug.Log("W键正在被按住");
        MoveForward();
    }
    
    // 释放瞬间触发（只执行一次）
    if (Input.GetKeyUp(KeyCode.LeftShift))
    {
        Debug.Log("左Shift键被释放");
        StopSprinting();
    }
}
```

---

### 2.2 KeyCode枚举与字符串参数

#### 使用KeyCode枚举（推荐）
```csharp
// 标准用法，类型安全，IDE支持
if (Input.GetKeyDown(KeyCode.W)) { /* ... */ }
if (Input.GetKey(KeyCode.LeftControl)) { /* ... */ }
if (Input.GetKeyUp(KeyCode.Escape)) { /* ... */ }
```

#### 使用字符串参数（兼容旧版本）
```csharp
// 字符串必须小写
if (Input.GetKeyDown("w")) { /* ... */ }
if (Input.GetKey("left ctrl")) { /* ... */ }
```

> 🌟 **KeyCode常用值**[ty-reference](6)：
> - **字母键**：`KeyCode.A` ~ `KeyCode.Z`
> - **数字键**：`KeyCode.Alpha0` ~ `KeyCode.Alpha9`
> - **功能键**：`KeyCode.Space`, `KeyCode.Return`, `KeyCode.Escape`
> - **修饰键**：`KeyCode.LeftShift`, `KeyCode.RightControl`, `KeyCode.LeftAlt`
> - **方向键**：`KeyCode.UpArrow`, `KeyCode.DownArrow`, `KeyCode.LeftArrow`, `KeyCode.RightArrow`

---

### 2.3 组合键检测

组合键需要同时检测多个按键的状态：

```csharp
void Update()
{
    // Ctrl + C 复制
    if (Input.GetKey(KeyCode.LeftControl) && Input.GetKeyDown(KeyCode.C))
    {
        Copy();
    }
    
    // Shift + 左键 瞄准
    if (Input.GetKey(KeyCode.LeftShift) && Input.GetMouseButtonDown(0))
    {
        Aim();
    }
    
    // W + A 斜向移动
    bool moveForward = Input.GetKey(KeyCode.W);
    bool moveLeft = Input.GetKey(KeyCode.A);
    if (moveForward && moveLeft)
    {
        MoveDiagonal(Vector3.forward + Vector3.left);
    }
}
```

---

## 三、鼠标输入详解

### 3.1 鼠标按键检测

鼠标按键使用数字参数标识，遵循标准约定：

| 参数值 | 对应按键 | 说明 |
|--------|----------|------|
| **0** | 左键 | 主要交互按键 |
| **1** | 右键 | 次要功能或上下文菜单 |
| **2** | 中键/滚轮 | 特殊功能或视角控制 |

```csharp
void Update()
{
    // 鼠标左键按下
    if (Input.GetMouseButtonDown(0))
    {
        Debug.Log("鼠标左键点击");
        Attack();
    }
    
    // 鼠标右键按住
    if (Input.GetMouseButton(1))
    {
        Debug.Log("鼠标右键长按");
        Zoom();
    }
    
    // 鼠标中键释放
    if (Input.GetMouseButtonUp(2))
    {
        Debug.Log("鼠标中键释放");
        ResetCamera();
    }
}
```

---

### 3.2 鼠标位置获取

#### Input.mousePosition
```csharp
// 获取鼠标屏幕坐标（左下角为原点）
Vector3 mousePos = Input.mousePosition;
Debug.Log($"鼠标位置: ({mousePos.x}, {mousePos.y}), 深度: {mousePos.z}");

// 实际应用：点击位置检测
void OnMouseDown()
{
    Vector3 clickPosition = Input.mousePosition;
    // 转换为世界坐标进行处理
}
```

> ⚠️ **重要特性**[ty-reference](1)：
> - 坐标系原点在**屏幕左下角**
> - X轴向右，Y轴向上
> - Z值恒为0（2D屏幕坐标）

---

### 3.3 鼠标滚轮检测

#### Input.mouseScrollDelta（推荐）
```csharp
void Update()
{
    // 获取滚轮增量（Y轴：1=向上滚动，-1=向下滚动）
    Vector2 scroll = Input.mouseScrollDelta;
    if (scroll.y > 0)
    {
        ZoomIn();
    }
    else if (scroll.y < 0)
    {
        ZoomOut();
    }
}
```

#### Input.GetAxis("Mouse ScrollWheel")（传统方式）
```csharp
// 返回值范围[-1, 1]，向上滚动为正
float scrollValue = Input.GetAxis("Mouse ScrollWheel");
if (scrollValue != 0)
{
    CameraZoom(scrollValue);
}
```

---

### 3.4 鼠标移动检测

#### 平滑移动轴（推荐用于摄像机控制）
```csharp
void Update()
{
    // 获取平滑的鼠标移动值（范围[-1, 1]）
    float mouseX = Input.GetAxis("Mouse X");
    float mouseY = Input.GetAxis("Mouse Y");
    
    // 应用于摄像机旋转
    transform.Rotate(-mouseY * sensitivity, mouseX * sensitivity, 0);
}
```

#### 离散移动轴（Raw输入）
```csharp
// 获取离散的鼠标移动值（-1, 0, 1）
float rawMouseX = Input.GetAxisRaw("Mouse X");
float rawMouseY = Input.GetAxisRaw("Mouse Y");
```

> 🌟 **Axis vs AxisRaw区别**[ty-reference](2)：
> - **GetAxis()**：有重力(Gravity)和灵敏度(Sensitivity)处理，值平滑变化
> - **GetAxisRaw()**：直接返回原始输入，值为-1/0/1，无平滑处理

---

## 四、高级输入技巧

### 4.1 任意键检测

当需要检测是否有任何输入发生时：

```csharp
void Update()
{
    // 检测当前是否有任何按键或鼠标按下
    if (Input.anyKey)
    {
        ShowCursor(false); // 隐藏光标
    }
    
    // 检测本帧是否有新的按键或鼠标按下
    if (Input.anyKeyDown)
    {
        StartGame(); // 开始游戏
    }
}
```

---

### 4.2 字符输入

对于文本输入场景，使用Input.inputString：

```csharp
public class TextInput : MonoBehaviour
{
    private string inputText = "";
    
    void Update()
    {
        // 获取当前帧输入的所有字符
        string newChars = Input.inputString;
        foreach (char c in newChars)
        {
            if (c == '\b') // 退格键
            {
                if (inputText.Length > 0)
                    inputText = inputText.Substring(0, inputText.Length - 1);
            }
            else if (c == '\n' || c == '\r') // 回车键
            {
                SubmitText(inputText);
                inputText = "";
            }
            else
            {
                inputText += c;
            }
        }
    }
}
```

---

### 4.3 输入轴配置

Unity的Input Manager允许自定义输入轴，位于**Edit → Project Settings → Input Manager**：

| 轴名称 | 默认绑定 | 代码调用 |
|--------|----------|----------|
| **Horizontal** | A/D, ←/→ | `Input.GetAxis("Horizontal")` |
| **Vertical** | W/S, ↑/↓ | `Input.GetAxis("Vertical")` |
| **Fire1** | 鼠标左键, Ctrl | `Input.GetButton("Fire1")` |
| **Jump** | Space | `Input.GetButtonDown("Jump")` |

```csharp
// 使用预定义轴进行角色移动
void Update()
{
    float horizontal = Input.GetAxis("Horizontal"); // -1到1
    float vertical = Input.GetAxis("Vertical");     // -1到1
    
    Vector3 movement = new Vector3(horizontal, 0, vertical);
    transform.Translate(movement * speed * Time.deltaTime);
    
    // 使用预定义按钮
    if (Input.GetButtonDown("Jump"))
    {
        Jump();
    }
}
```

---

## 五、实用示例

### 5.1 完整的角色控制器

```csharp
public class PlayerController : MonoBehaviour
{
    public float moveSpeed = 5f;
    public float rotationSpeed = 2f;
    public float jumpForce = 5f;
    
    private CharacterController controller;
    
    void Start()
    {
        controller = GetComponent<CharacterController>();
    }
    
    void Update()
    {
        // 键盘移动
        float horizontal = Input.GetAxis("Horizontal");
        float vertical = Input.GetAxis("Vertical");
        Vector3 movement = transform.right * horizontal + transform.forward * vertical;
        controller.Move(movement * moveSpeed * Time.deltaTime);
        
        // 鼠标旋转
        float mouseX = Input.GetAxis("Mouse X") * rotationSpeed;
        transform.Rotate(0, mouseX, 0);
        
        // 空格键跳跃
        if (Input.GetButtonDown("Jump") && controller.isGrounded)
        {
            controller.Move(Vector3.up * jumpForce);
        }
        
        // 鼠标左键攻击
        if (Input.GetMouseButtonDown(0))
        {
            Attack();
        }
        
        // 鼠标滚轮切换武器
        float scroll = Input.mouseScrollDelta.y;
        if (scroll > 0) SwitchWeaponNext();
        else if (scroll < 0) SwitchWeaponPrevious();
    }
}
```

---

### 5.2 摄像机控制

```csharp
public class CameraController : MonoBehaviour
{
    public Transform target;
    public float sensitivity = 2f;
    public float zoomSpeed = 5f;
    
    private float xRotation = 0f;
    private float minAngle = -60f;
    private float maxAngle = 60f;
    
    void LateUpdate()
    {
        if (target != null)
        {
            // 鼠标右键控制视角
            if (Input.GetMouseButton(1))
            {
                float mouseX = Input.GetAxis("Mouse X") * sensitivity;
                float mouseY = Input.GetAxis("Mouse Y") * sensitivity;
                
                // 垂直旋转限制
                xRotation -= mouseY;
                xRotation = Mathf.Clamp(xRotation, minAngle, maxAngle);
                
                // 应用旋转
                transform.localRotation = Quaternion.Euler(xRotation, 0f, 0f);
                target.Rotate(Vector3.up * mouseX);
            }
            
            // 鼠标滚轮缩放
            float scroll = Input.GetAxis("Mouse ScrollWheel");
            if (scroll != 0)
            {
                // 缩放逻辑...
            }
        }
    }
}
```

---

## 六、最佳实践与常见错误

### 6.1 性能考虑

| 操作 | 性能 | 建议 |
|------|------|------|
| GetKey系列 | 高 | 可在Update中安全使用 |
| GetAxis系列 | 高 | 适合连续输入检测 |
| Input.mousePosition | 高 | 直接访问属性，无额外开销 |
| 频繁字符串比较 | 低 | 优先使用KeyCode枚举 |

> 💡 **最佳实践**：
> - **优先使用KeyCode枚举**而非字符串
> - **合理使用GetAxis**处理连续输入
> - **避免在FixedUpdate中检测瞬时输入**
> - **组合键检测要注意按键顺序**

---

### 6.2 常见错误与解决方案

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 按键检测不灵敏 | 在FixedUpdate中检测 | 改到Update中检测 |
| 组合键失效 | 没有正确检测多个按键 | 同时检测所有必要按键 |
| 鼠标坐标错误 | 忘记坐标系原点位置 | 记住左下角为(0,0) |
| 滚轮检测失败 | 使用了错误的API | 优先使用mouseScrollDelta |
| 输入延迟 | 在LateUpdate中检测输入 | 在Update中检测输入 |

---

### 6.3 调试技巧

```csharp
// 输入调试辅助类
public static class InputDebugger
{
    public static void LogActiveInputs()
    {
        // 检测所有方向键
        if (Input.anyKey) Debug.Log("有输入活动");
        if (Input.GetMouseButton(0)) Debug.Log("鼠标左键按下");
        if (Input.GetKey(KeyCode.W)) Debug.Log("W键按下");
        
        // 显示鼠标位置
        Debug.Log($"鼠标位置: {Input.mousePosition}");
        
        // 显示轴值
        Debug.Log($"水平轴: {Input.GetAxis("Horizontal")}, 垂直轴: {Input.GetAxis("Vertical")}");
    }
}
```

---

## 七、总结：Input系统黄金法则

1. **检测时机**：输入检测放在Update()方法中
2. **方法选择**：GetKeyDown(瞬间)、GetKey(持续)、GetKeyUp(释放)
3. **参数规范**：优先使用KeyCode枚举，避免字符串
4. **鼠标处理**：记住屏幕坐标系原点在左下角
5. **轴的使用**：连续输入用GetAxis，瞬时输入用GetButton
6. **组合键**：同时检测所有必要按键的状态
7. **性能优化**：避免不必要的字符串操作和重复检测

> ✨ **终极提示**：  
> "Input系统是玩家与游戏世界的桥梁，  
> 掌握鼠标键盘输入的精髓，理解不同检测方法的适用场景，  
> 你就能创造出**响应灵敏、操作流畅**的游戏体验；  
> 用好这些技巧，你的游戏将拥有**直观而愉悦**的用户交互！"