# Unity 屏幕相关（Screen）知识点详解

## 一、Screen类基础概念

### 1.1 Screen类的作用与定位

Unity 的 **Screen 类**（位于 `UnityEngine` 命名空间）是管理应用程序**显示窗口和屏幕属性**的核心工具。它提供了对屏幕分辨率、全屏模式、屏幕方向等关键显示参数的访问和控制能力。

> 💡 **核心理解**：  
> Screen 类让你能够**动态适配不同设备的显示特性**，确保游戏在各种分辨率、屏幕比例和设备类型上都能提供最佳视觉体验[ty-reference](1)。

---

### 1.2 Screen类的主要功能

| 功能类别 | 具体能力 | 应用场景 |
|----------|----------|----------|
| **分辨率管理** | 获取/设置屏幕分辨率 | 自适应UI布局、性能优化 |
| **全屏控制** | 切换全屏/窗口模式 | 沉浸式游戏体验 |
| **屏幕方向** | 控制屏幕旋转方向 | 移动端横竖屏适配 |
| **DPI信息** | 获取屏幕像素密度 | 高清UI适配 |
| **安全区域** | 获取刘海屏安全区域 | 现代手机UI适配 |

---

## 二、屏幕分辨率与尺寸

### 2.1 获取屏幕基本信息

#### Screen.width 和 Screen.height
```csharp
void DisplayScreenInfo()
{
    // 获取当前屏幕分辨率
    int screenWidth = Screen.width;   // 屏幕宽度（像素）
    int screenHeight = Screen.height; // 屏幕高度（像素）
    
    Debug.Log($"当前分辨率: {screenWidth} x {screenHeight}");
    
    // 实际应用：动态调整UI元素位置
    Vector2 centerPosition = new Vector2(screenWidth / 2f, screenHeight / 2f);
    uiPanel.anchoredPosition = centerPosition;
}
```

#### Screen.currentResolution
```csharp
// 获取当前显示器的原生分辨率
Resolution currentRes = Screen.currentResolution;
Debug.Log($"原生分辨率: {currentRes.width} x {currentRes.height} @ {currentResolution.refreshRate}Hz");
```

> ⚠️ **重要区别**[ty-reference](1)：
> - **Screen.width/height**：返回**游戏窗口的实际分辨率**
> - **Screen.currentResolution**：返回**显示器的物理分辨率**

---

### 2.2 支持的分辨率列表

#### 获取所有支持的分辨率
```csharp
public class ResolutionManager : MonoBehaviour
{
    void Start()
    {
        // 获取所有支持的分辨率
        Resolution[] resolutions = Screen.resolutions;
        
        Debug.Log($"支持的分辨率数量: {resolutions.Length}");
        
        foreach (Resolution res in resolutions)
        {
            Debug.Log($"{res.width} x {res.height} @ {res.refreshRate}Hz");
        }
        
        // 显示最高分辨率选项
        if (resolutions.Length > 0)
        {
            Resolution maxRes = resolutions[resolutions.Length - 1];
            Debug.Log($"最高分辨率: {maxRes.width} x {maxRes.height}");
        }
    }
}
```

#### 设置特定分辨率
```csharp
public void SetResolution(int width, int height, bool fullscreen)
{
    // 检查分辨率是否受支持
    Resolution[] supportedResolutions = Screen.resolutions;
    bool isSupported = false;
    
    foreach (Resolution res in supportedResolutions)
    {
        if (res.width == width && res.height == height)
        {
            isSupported = true;
            break;
        }
    }
    
    if (isSupported)
    {
        Screen.SetResolution(width, height, fullscreen);
        Debug.Log($"分辨率已设置为: {width} x {height}");
    }
    else
    {
        Debug.LogWarning($"分辨率 {width} x {height} 不受支持！");
    }
}
```

---

## 三、全屏模式控制

### 3.1 全屏模式类型

Unity 支持多种全屏模式，通过 **FullScreenMode 枚举**进行控制：

| 模式 | 说明 | 特点 |
|------|------|------|
| **Windowed** | 窗口模式 | 可调整窗口大小，有边框 |
| **ExclusiveFullScreen** | 独占全屏 | 最高性能，切换时黑屏 |
| **FullscreenWindow** | 全屏窗口 | 无边框窗口，快速切换 |
| **MaximizedWindow** | 最大化窗口 | 占满屏幕但保留任务栏 |

```csharp
// 设置不同的全屏模式
public void SetFullScreenMode(FullScreenMode mode)
{
    Screen.fullScreenMode = mode;
    
    switch (mode)
    {
        case FullScreenMode.Windowed:
            Debug.Log("切换到窗口模式");
            break;
        case FullScreenMode.ExclusiveFullScreen:
            Debug.Log("切换到独占全屏模式");
            break;
        case FullScreenMode.FullScreenWindow:
            Debug.Log("切换到全屏窗口模式");
            break;
        case FullScreenMode.MaximizedWindow:
            Debug.Log("切换到最大化窗口模式");
            break;
    }
}
```

---

### 3.2 动态全屏切换

#### 创建全屏切换按钮
```csharp
public class FullScreenToggle : MonoBehaviour
{
    public void ToggleFullScreen()
    {
        if (Screen.fullScreenMode == FullScreenMode.Windowed)
        {
            // 切换到全屏窗口模式（推荐）
            Screen.fullScreenMode = FullScreenMode.FullScreenWindow;
        }
        else
        {
            // 切换回窗口模式
            Screen.fullScreenMode = FullScreenMode.Windowed;
        }
        
        UpdateUIButtonText();
    }
    
    private void UpdateUIButtonText()
    {
        string buttonText = Screen.fullScreenMode == FullScreenMode.Windowed ? "全屏" : "窗口";
        // 假设有一个Text组件来显示按钮文字
        // fullScreenButtonText.text = buttonText;
    }
}
```

> 🌟 **最佳实践**：  
> 推荐使用 **FullScreenWindow** 模式，因为它提供了**快速切换**和**良好兼容性**，避免了独占全屏模式的黑屏问题[ty-reference](2)。

---

## 四、移动端屏幕方向控制

### 4.1 屏幕方向枚举

对于移动平台，Screen 类提供了精细的屏幕方向控制：

| 方向 | 说明 | 适用场景 |
|------|------|----------|
| **Portrait** | 竖屏（Home键在下） | 手机游戏、聊天应用 |
| **PortraitUpsideDown** | 倒置竖屏 | 特殊交互需求 |
| **LandscapeLeft** | 横屏（Home键在左） | 赛车游戏、视频播放 |
| **LandscapeRight** | 横屏（Home键在右） | 平台游戏、射击游戏 |
| **AutoRotation** | 自动旋转 | 通用应用 |
| **Unknown** | 未知方向 | 错误状态 |

```csharp
// 强制设置屏幕方向
public void SetPortraitMode()
{
    Screen.orientation = ScreenOrientation.Portrait;
}

public void SetLandscapeMode()
{
    Screen.orientation = ScreenOrientation.LandscapeLeft;
}
```

---

### 4.2 自动旋转配置

#### 启用特定方向的自动旋转
```csharp
public class OrientationManager : MonoBehaviour
{
    void Start()
    {
        // 允许竖屏和横屏自动旋转
        Screen.autorotateToPortrait = true;
        Screen.autorotateToPortraitUpsideDown = false;
        Screen.autorotateToLandscapeLeft = true;
        Screen.autorotateToLandscapeRight = true;
        
        // 设置默认方向
        Screen.orientation = ScreenOrientation.AutoRotation;
    }
}
```

#### 根据设备类型设置方向
```csharp
void ConfigureOrientationForDevice()
{
    if (Application.isMobilePlatform)
    {
        // 移动设备：根据游戏类型选择方向
        if (IsPortraitGame())
        {
            Screen.orientation = ScreenOrientation.Portrait;
        }
        else
        {
            Screen.orientation = ScreenOrientation.LandscapeLeft;
        }
    }
    else
    {
        // PC/Mac：通常不需要强制方向
        Screen.orientation = ScreenOrientation.Unknown;
    }
}
```

---

## 五、高级屏幕特性

### 5.1 DPI（像素密度）处理

#### 获取屏幕DPI信息
```csharp
void HandleDPIScaling()
{
    float dpi = Screen.dpi;
    
    if (dpi > 0)
    {
        Debug.Log($"屏幕DPI: {dpi}");
        
        // 根据DPI调整UI缩放
        if (dpi >= 300) // 高DPI设备（如Retina显示屏）
        {
            // 启用高分辨率UI
            EnableHighResolutionUI();
        }
        else
        {
            // 标准分辨率UI
            EnableStandardResolutionUI();
        }
    }
    else
    {
        Debug.LogWarning("无法获取DPI信息，使用默认设置");
        EnableStandardResolutionUI();
    }
}
```

> ⚠️ **注意**：某些平台可能返回DPI为0，需要做好兼容处理[ty-reference](1)。

---

### 5.2 安全区域（Safe Area）

现代移动设备（如iPhone X系列）具有**刘海屏或挖孔屏**，需要考虑安全区域：

```csharp
using UnityEngine;

public class SafeAreaHandler : MonoBehaviour
{
    private RectTransform panel;
    private Rect lastSafeArea = new Rect();
    
    void Start()
    {
        panel = GetComponent<RectTransform>();
        ApplySafeArea();
    }
    
    void Update()
    {
        // 检测安全区域是否发生变化
        if (lastSafeArea != Screen.safeArea)
        {
            ApplySafeArea();
        }
    }
    
    void ApplySafeArea()
    {
        Rect safeArea = Screen.safeArea;
        Vector2 anchorMin = safeArea.position;
        Vector2 anchorMax = safeArea.position + safeArea.size;
        
        // 转换为相对于屏幕的归一化坐标
        anchorMin.x /= Screen.width;
        anchorMin.y /= Screen.height;
        anchorMax.x /= Screen.width;
        anchorMax.y /= Screen.height;
        
        panel.anchorMin = anchorMin;
        panel.anchorMax = anchorMax;
        
        lastSafeArea = safeArea;
    }
}
```

> 💡 **安全区域作用**：  
> 确保UI元素不会被**刘海、挖孔或圆角**遮挡，提供完整的用户交互体验[ty-reference](3)。

---

### 5.3 刷新率控制

#### 获取和设置刷新率
```csharp
public class RefreshRateManager : MonoBehaviour
{
    void Start()
    {
        // 获取当前刷新率
        int currentRefreshRate = Screen.currentResolution.refreshRate;
        Debug.Log($"当前刷新率: {currentRefreshRate}Hz");
        
        // 获取目标帧率（通常由Application.targetFrameRate设置）
        int targetFrameRate = Application.targetFrameRate;
        Debug.Log($"目标帧率: {targetFrameRate}FPS");
        
        // 优化建议：如果设备支持高刷新率，可以提升游戏流畅度
        if (currentRefreshRate >= 90)
        {
            // 启用高帧率模式
            Application.targetFrameRate = 90;
        }
    }
}
```

---

## 六、实用应用场景

### 6.1 分辨率自适应UI系统

```csharp
public class AdaptiveUISystem : MonoBehaviour
{
    [SerializeField] private CanvasScaler canvasScaler;
    
    void Start()
    {
        SetupCanvasScaler();
    }
    
    void SetupCanvasScaler()
    {
        if (canvasScaler != null)
        {
            // 根据屏幕分辨率设置Canvas缩放模式
            float aspectRatio = (float)Screen.width / Screen.height;
            
            if (aspectRatio > 2.0f) // 超宽屏
            {
                canvasScaler.matchWidthOrHeight = 0f; // 优先匹配宽度
            }
            else if (aspectRatio < 1.0f) // 竖屏
            {
                canvasScaler.matchWidthOrHeight = 1f; // 优先匹配高度
            }
            else // 标准比例
            {
                canvasScaler.matchWidthOrHeight = 0.5f; // 平衡匹配
            }
        }
    }
}
```

---

### 6.2 动态分辨率性能优化

```csharp
public class DynamicResolutionManager : MonoBehaviour
{
    [Range(0.5f, 1.0f)] public float resolutionScale = 1.0f;
    
    void Update()
    {
        // 根据性能动态调整分辨率
        if (ShouldReduceResolution())
        {
            resolutionScale = Mathf.Max(0.5f, resolutionScale - 0.01f);
        }
        else if (ShouldIncreaseResolution())
        {
            resolutionScale = Mathf.Min(1.0f, resolutionScale + 0.01f);
        }
        
        // 应用分辨率缩放
        int targetWidth = Mathf.RoundToInt(Screen.currentResolution.width * resolutionScale);
        int targetHeight = Mathf.RoundToInt(Screen.currentResolution.height * resolutionScale);
        
        Screen.SetResolution(targetWidth, targetHeight, Screen.fullScreen);
    }
    
    bool ShouldReduceResolution()
    {
        // 示例：当帧率低于30FPS时降低分辨率
        return Time.unscaledDeltaTime > 1f / 30f;
    }
    
    bool ShouldIncreaseResolution()
    {
        // 示例：当帧率稳定在60FPS以上时提高分辨率
        return Time.unscaledDeltaTime < 1f / 60f;
    }
}
```

---

### 6.3 移动端横竖屏切换处理

```csharp
public class MobileOrientationHandler : MonoBehaviour
{
    private Camera mainCamera;
    private float originalOrthographicSize;
    
    void Start()
    {
        mainCamera = Camera.main;
        if (mainCamera != null && mainCamera.orthographic)
        {
            originalOrthographicSize = mainCamera.orthographicSize;
        }
    }
    
    void Update()
    {
        HandleOrientationChange();
    }
    
    void HandleOrientationChange()
    {
        if (Application.isMobilePlatform)
        {
            // 根据屏幕方向调整摄像机
            float aspectRatio = (float)Screen.width / Screen.height;
            
            if (mainCamera != null && mainCamera.orthographic)
            {
                // 保持游戏内容完整显示
                mainCamera.orthographicSize = originalOrthographicSize * 
                    (aspectRatio > 1 ? aspectRatio : 1f / aspectRatio);
            }
        }
    }
}
```

---

## 七、最佳实践与常见错误

### 7.1 性能考虑

| 操作 | 性能影响 | 建议 |
|------|----------|------|
| **频繁调用SetResolution** | 高（可能导致卡顿） | 缓存当前分辨率，避免重复设置 |
| **每帧检查Screen属性** | 低 | 可以安全使用 |
| **DPI计算** | 低 | 一次性计算，缓存结果 |
| **安全区域检测** | 低 | 在Update中检测变化即可 |

> 💡 **最佳实践**：
> - **分辨率设置**：只在必要时调用SetResolution
> - **UI适配**：优先使用Canvas Scaler而非手动计算
> - **移动端方向**：明确指定支持的方向，避免不必要的旋转
> - **安全区域**：所有重要UI都应该考虑安全区域适配

---

### 7.2 常见错误与解决方案

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 分辨率设置无效 | 分辨率不受支持 | 先检查Screen.resolutions |
| 全屏切换黑屏 | 使用了独占全屏模式 | 改用FullScreenWindow模式 |
| UI被刘海遮挡 | 未处理安全区域 | 实现SafeAreaHandler |
| 移动端方向混乱 | 未明确设置orientation | 在Start中设置固定方向 |
| DPI返回0 | 平台不支持DPI查询 | 提供默认DPI值（如96） |

---

### 7.3 跨平台兼容性

```csharp
public class CrossPlatformScreenManager : MonoBehaviour
{
    void Start()
    {
        if (Application.isMobilePlatform)
        {
            // 移动平台特殊处理
            ConfigureMobileScreen();
        }
        else if (Application.isEditor)
        {
            // 编辑器中使用开发分辨率
            Screen.SetResolution(1920, 1080, false);
        }
        else
        {
            // PC/Mac平台处理
            ConfigureDesktopScreen();
        }
    }
    
    void ConfigureMobileScreen()
    {
        // 强制竖屏
        Screen.orientation = ScreenOrientation.Portrait;
        
        // 启用安全区域处理
        gameObject.AddComponent<SafeAreaHandler>();
    }
    
    void ConfigureDesktopScreen()
    {
        // 默认窗口模式
        Screen.fullScreenMode = FullScreenMode.Windowed;
        
        // 如果需要，可以读取玩家偏好设置
        LoadPlayerScreenPreferences();
    }
}
```

---

## 八、总结：Screen类黄金法则

1. **分辨率适配**：使用Screen.width/height获取实际游戏分辨率，合理设置Canvas Scaler
2. **全屏策略**：优先使用FullScreenWindow模式，提供良好的用户体验
3. **移动端方向**：明确指定Screen.orientation，避免自动旋转带来的问题
4. **安全区域**：现代移动设备必须考虑Screen.safeArea，确保UI完整性
5. **DPI处理**：根据屏幕像素密度调整UI细节，提供高清体验
6. **性能优化**：避免频繁调用SetResolution，合理使用动态分辨率
7. **跨平台兼容**：根据不同平台特性提供针对性的屏幕配置

> ✨ **终极提示**：  
> "屏幕是玩家与游戏世界的唯一窗口，  
> 掌握Screen类的精髓，理解不同平台的显示特性，  
> 你就能创造出**完美适配、视觉震撼**的游戏体验；  
> 用好这些技巧，你的游戏将真正实现**全设备无缝运行**！"