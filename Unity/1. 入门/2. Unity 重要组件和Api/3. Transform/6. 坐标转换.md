# Unity Transform坐标转换详解

## 一、坐标转换基础概念

### 1.1 为什么需要坐标转换

在Unity开发中，我们经常遇到这样的场景：
- **鼠标点击屏幕**，需要知道在3D世界中的具体位置
- **敌人AI** 需要判断玩家是否在其视野范围内
- **UI元素** 需要显示在3D物体的正上方
- **子对象** 需要知道自己在世界空间中的实际位置

这些需求都涉及到**不同坐标系之间的转换**。Unity提供了完整的坐标转换API，让我们能够轻松地在各种空间之间进行转换。

> 💡 **核心理解**：  
> 坐标转换的本质是**将一个点或向量从一个坐标系映射到另一个坐标系**，确保数据在不同参考系下的一致性和准确性。

---

### 1.2 Unity中的主要坐标系

| 坐标系                       | 描述                  | 特点             |
| ------------------------- | ------------------- | -------------- |
| **世界坐标系（World Space）**    | 相对于场景原点(0,0,0)的绝对坐标 | 所有对象共享同一套坐标系   |
| **本地坐标系（Local Space）**    | 相对于父对象原点的相对坐标       | 每个对象都有自己的本地坐标系 |
| **屏幕坐标系（Screen Space）**   | 以像素为单位的2D屏幕坐标       | 原点在左下角，Z值表示深度  |
| **视口坐标系（Viewport Space）** | 归一化的屏幕坐标            | 范围[0,1]，与分辨率无关 |
|                           |                     |                |

```csharp
// 各坐标系示例
Vector3 worldPos = transform.position;           // 世界坐标
Vector3 localPos = transform.localPosition;      // 本地坐标
Vector3 screenPos = Camera.main.WorldToScreenPoint(worldPos); // 屏幕坐标
Vector3 viewportPos = Camera.main.WorldToViewportPoint(worldPos); // 视口坐标
```

---

## 二、Transform内置坐标转换方法

### 2.1 世界坐标 ↔ 本地坐标转换

#### TransformPoint / InverseTransformPoint（点的转换）
```csharp
// 世界坐标转本地坐标
Vector3 worldPoint = new Vector3(5, 0, 10);
Vector3 localPoint = transform.InverseTransformPoint(worldPoint);

// 本地坐标转世界坐标
Vector3 localPoint2 = new Vector3(1, 0, 0);
Vector3 worldPoint2 = transform.TransformPoint(localPoint2);
```

> 🌟 **适用场景**：位置、顶点等**有具体位置意义**的数据

---

#### TransformDirection / InverseTransformDirection（方向的转换）
```csharp
// 世界方向转本地方向
Vector3 worldDirection = Vector3.forward;
Vector3 localDirection = transform.InverseTransformDirection(worldDirection);

// 本地方向转世界方向
Vector3 localDirection2 = Vector3.right;
Vector3 worldDirection2 = transform.TransformDirection(localDirection2);
```

> 🌟 **适用场景**：法线、朝向等**只有方向意义**的数据（不受缩放影响）

---

#### TransformVector / InverseTransformVector（向量的转换）
```csharp
// 世界向量转本地向量
Vector3 worldVector = new Vector3(1, 1, 0);
Vector3 localVector = transform.InverseTransformVector(worldVector);

// 本地向量转世界向量
Vector3 localVector2 = new Vector3(0, 1, 0);
Vector3 worldVector2 = transform.TransformVector(localVector2);
```

> 🌟 **适用场景**：速度、力等**既有方向又有大小**的数据（受缩放影响）

---

### 2.2 关键区别：Point vs Direction vs Vector

| 方法类型 | 是否受位置影响 | 是否受旋转影响 | 是否受缩放影响 | 典型用途 |
|----------|----------------|----------------|----------------|----------|
| **Point** | ✅ | ✅ | ✅ | 位置、顶点坐标 |
| **Direction** | ❌ | ✅ | ❌ | 法线、朝向、光线方向 |
| **Vector** | ❌ | ✅ | ✅ | 速度、加速度、力 |

```csharp
// 示例：不同转换方法的效果
void DemonstrateConversionDifferences()
{
    Transform obj = transform;
    
    // 点的转换（包含位置偏移）
    Vector3 worldPoint = new Vector3(10, 0, 0);
    Vector3 localPoint = obj.InverseTransformPoint(worldPoint);
    
    // 方向的转换（忽略位置，不受缩放影响）
    Vector3 worldDir = Vector3.forward;
    Vector3 localDir = obj.InverseTransformDirection(worldDir);
    
    // 向量的转换（忽略位置，受缩放影响）
    Vector3 worldVec = Vector3.up * 5f;
    Vector3 localVec = obj.InverseTransformVector(worldVec);
}
```

---

## 三、Camera坐标转换方法

### 3.1 世界坐标 ↔ 屏幕/视口坐标

#### WorldToScreenPoint / ScreenToWorldPoint
```csharp
// 世界坐标转屏幕坐标
Vector3 worldPos = target.position;
Vector3 screenPos = Camera.main.WorldToScreenPoint(worldPos);
Debug.Log($"屏幕坐标: ({screenPos.x}, {screenPos.y}), 深度: {screenPos.z}");

// 屏幕坐标转世界坐标
Vector3 mouseScreenPos = new Vector3(Input.mousePosition.x, Input.mousePosition.y, distance);
Vector3 worldMousePos = Camera.main.ScreenToWorldPoint(mouseScreenPos);
```

#### WorldToViewportPoint / ViewportToWorldPoint
```csharp
// 世界坐标转视口坐标（归一化）
Vector3 viewportPos = Camera.main.WorldToViewportPoint(worldPos);
// viewportPos.x 和 viewportPos.y 范围 [0,1]

// 视口坐标转世界坐标
Vector3 viewportInput = new Vector3(0.5f, 0.5f, distance); // 屏幕中心
Vector3 worldFromViewport = Camera.main.ViewportToWorldPoint(viewportInput);
```

> ⚠️ **重要提示**：屏幕和视口坐标的**Z值表示深度**，转换时必须提供正确的Z值！

---

### 3.2 实用屏幕坐标转换示例

#### 鼠标点击射线检测
```csharp
public class MouseRaycast : MonoBehaviour
{
    void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            // 获取鼠标屏幕位置
            Vector3 mousePos = Input.mousePosition;
            
            // 创建射线
            Ray ray = Camera.main.ScreenPointToRay(mousePos);
            
            // 射线检测
            if (Physics.Raycast(ray, out RaycastHit hit))
            {
                Debug.Log($"点击了: {hit.collider.name}");
                // 移动到点击位置
                transform.position = hit.point;
            }
        }
    }
}
```

#### UI跟随3D物体
```csharp
public class WorldSpaceUI : MonoBehaviour
{
    public Transform target;
    public Canvas canvas;
    
    void LateUpdate()
    {
        if (target != null && canvas != null)
        {
            // 将3D世界坐标转换为UI的RectTransform坐标
            Vector3 worldPos = target.position + Vector3.up * 2f; // 在目标上方2单位
            Vector3 screenPos = Camera.main.WorldToScreenPoint(worldPos);
            
            // 设置UI位置（假设Canvas是Screen Space - Overlay）
            RectTransform rectTransform = GetComponent<RectTransform>();
            rectTransform.position = screenPos;
        }
    }
}
```

---

## 四、高级坐标转换技巧

### 4.1 多层级坐标转换

当对象处于复杂的父子层级中时，可能需要跨多个层级进行转换：

```csharp
// 从孙子对象获取相对于爷爷对象的坐标
public Transform grandChild;
public Transform grandParent;

void ConvertAcrossHierarchy()
{
    // 方法1：逐步转换
    Vector3 worldPos = grandChild.position;
    Vector3 localToGrandParent = grandParent.InverseTransformPoint(worldPos);
    
    // 方法2：直接使用世界坐标作为中介
    Vector3 result = grandParent.InverseTransformPoint(grandChild.position);
}
```

---

### 4.2 自定义坐标系转换

有时需要在两个任意对象之间进行坐标转换：

```csharp
public static class TransformExtensions
{
    /// <summary>
    /// 将source坐标系中的点转换到target坐标系中
    /// </summary>
    public static Vector3 TransformPointBetween(this Transform source, Vector3 point, Transform target)
    {
        // 先转到世界坐标，再转到目标坐标系
        Vector3 worldPoint = source.TransformPoint(point);
        return target.InverseTransformPoint(worldPoint);
    }
    
    /// <summary>
    /// 将source坐标系中的方向转换到target坐标系中
    /// </summary>
    public static Vector3 TransformDirectionBetween(this Transform source, Vector3 direction, Transform target)
    {
        Vector3 worldDirection = source.TransformDirection(direction);
        return target.InverseTransformDirection(worldDirection);
    }
}

// 使用示例
Vector3 localPointInA = new Vector3(1, 0, 0);
Vector3 pointInB = transformA.TransformPointBetween(localPointInA, transformB);
```

---

### 4.3 性能优化的坐标转换

避免在Update中重复创建不必要的对象：

```csharp
public class OptimizedCoordinateConversion : MonoBehaviour
{
    private Camera mainCamera;
    private Vector3 reusableVector = Vector3.zero;
    
    void Start()
    {
        mainCamera = Camera.main;
    }
    
    void Update()
    {
        // 重用Vector3对象，避免GC
        reusableVector.Set(Input.mousePosition.x, Input.mousePosition.y, 10f);
        Vector3 worldPos = mainCamera.ScreenToWorldPoint(reusableVector);
        
        // 或者直接使用Input.mousePosition（它是属性，内部已优化）
        Vector3 worldPos2 = mainCamera.ScreenToWorldPoint(new Vector3(Input.mousePosition.x, Input.mousePosition.y, 10f));
    }
}
```

---

## 五、实用应用场景

### 5.1 第三人称摄像机

```csharp
public class ThirdPersonCamera : MonoBehaviour
{
    public Transform target;
    public Vector3 offset = new Vector3(0, 2, -5);
    public float smoothSpeed = 0.125f;
    
    void LateUpdate()
    {
        if (target != null)
        {
            // 计算目标位置（在目标本地坐标系中的偏移）
            Vector3 desiredPosition = target.TransformPoint(offset);
            
            // 平滑移动到目标位置
            Vector3 smoothedPosition = Vector3.Lerp(transform.position, desiredPosition, smoothSpeed);
            transform.position = smoothedPosition;
            
            // 看向目标
            transform.LookAt(target);
        }
    }
}
```

---

### 5.2 子弹轨迹预测

```csharp
public class BulletPrediction : MonoBehaviour
{
    public Transform shooter;
    public Transform target;
    public float bulletSpeed = 100f;
    
    void CalculateLeadPosition()
    {
        if (shooter != null && target != null)
        {
            // 获取目标在射手本地坐标系中的位置
            Vector3 targetLocalPos = shooter.InverseTransformPoint(target.position);
            
            // 计算飞行时间（简化版）
            float distance = targetLocalPos.magnitude;
            float flightTime = distance / bulletSpeed;
            
            // 预测目标未来位置（假设目标匀速移动）
            Vector3 targetVelocity = shooter.InverseTransformDirection(target.GetComponent<Rigidbody>().velocity);
            Vector3 predictedLocalPos = targetLocalPos + targetVelocity * flightTime;
            
            // 转换回世界坐标进行射击
            Vector3 predictedWorldPos = shooter.TransformPoint(predictedLocalPos);
            
            // 射击逻辑...
        }
    }
}
```

---

### 5.3 调试可视化

```csharp
public class CoordinateSystemDebugger : MonoBehaviour
{
    public Transform referenceObject;
    
    void OnDrawGizmos()
    {
        if (referenceObject != null)
        {
            // 绘制本地坐标系轴
            Gizmos.color = Color.red;
            Gizmos.DrawLine(referenceObject.position, referenceObject.TransformPoint(Vector3.right * 2f));
            
            Gizmos.color = Color.green;
            Gizmos.DrawLine(referenceObject.position, referenceObject.TransformPoint(Vector3.up * 2f));
            
            Gizmos.color = Color.blue;
            Gizmos.DrawLine(referenceObject.position, referenceObject.TransformPoint(Vector3.forward * 2f));
        }
    }
}
```

---

## 六、最佳实践与常见错误

### 6.1 性能考虑

| 操作               | 性能  | 建议                           |
| ---------------- | --- | ---------------------------- |
| TransformPoint系列 | 高   | 可在Update中安全使用                |
| Camera坐标转换       | 中   | 缓存Camera引用，避免重复调用Camera.main |
| 射线检测             | 中   | 限制检测频率，使用LayerMask优化         |
| 频繁对象创建           | 低   | 重用Vector3对象，避免GC             |

> 💡 **最佳实践**：
> - **缓存Camera引用**：`private Camera mainCamera;`
> - **选择正确的转换方法**：Point/Direction/Vector根据数据类型选择
> - **避免不必要的转换**：只在真正需要时进行坐标转换
> - **使用LateUpdate处理摄像机相关转换**：确保目标对象已更新

---

### 6.2 常见错误与解决方案

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 屏幕坐标转换结果为NaN | Z值为0或负数 | 提供正确的深度值（distance > 0） |
| 转换结果不准确 | 使用了错误的转换方法 | 根据数据类型选择Point/Direction/Vector |
| 性能下降 | 频繁调用Camera.main | 缓存Camera引用 |
| 坐标系混淆 | 不清楚各坐标系的区别 | 明确区分World/Local/Screen/Viewport |
| 射线检测失败 | 没有提供正确的Z值 | 使用Physics.Raycast或正确设置深度 |

---

### 6.3 调试技巧

```csharp
// 调试坐标转换的实用方法
public static class DebugUtils
{
    public static void LogCoordinateSystems(Transform obj)
    {
        Debug.Log($"=== {obj.name} 坐标系统 ===");
        Debug.Log($"世界位置: {obj.position}");
        Debug.Log($"本地位置: {obj.localPosition}");
        Debug.Log($"世界旋转: {obj.rotation.eulerAngles}");
        Debug.Log($"本地旋转: {obj.localRotation.eulerAngles}");
        Debug.Log($"世界缩放: {obj.lossyScale}"); // 注意：lossyScale是只读的
        Debug.Log($"本地缩放: {obj.localScale}");
    }
}
```

---

## 七、总结：坐标转换黄金法则

1. **理解坐标系**：明确World/Local/Screen/Viewport的区别和用途
2. **选择正确方法**：Point用于位置，Direction用于方向，Vector用于向量
3. **Camera转换要点**：屏幕坐标转换必须提供正确的Z深度值
4. **性能优化**：缓存Camera引用，避免重复对象创建
5. **层级处理**：复杂层级中以世界坐标为中介进行转换
6. **调试验证**：使用Gizmos和日志验证转换结果的正确性
7. **时机选择**：摄像机相关逻辑放在LateUpdate中执行

> ✨ **终极提示**：  
> "Transform坐标转换是Unity空间计算的桥梁，  
> 掌握不同坐标系的特点，熟练运用转换方法，  
> 你就能让2D与3D、本地与世界、屏幕与场景**无缝衔接**；  
> 用好这些技巧，你的游戏将拥有**精准而流畅**的空间交互体验！"