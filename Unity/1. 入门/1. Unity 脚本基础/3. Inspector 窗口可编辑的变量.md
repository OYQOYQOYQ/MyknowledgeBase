# Unity Inspector窗口可编辑变量详解

## 一、什么是Inspector窗口可编辑的变量？

Inspector窗口是Unity中用于查看和编辑游戏对象属性的核心界面。**Inspector窗口显示的可编辑内容本质上就是脚本的成员变量**。通过在脚本中声明特定类型的变量，开发者可以让这些变量在Inspector窗口中显示，从而在不修改代码的情况下调整游戏参数。

> 💡 **核心概念**：Inspector窗口中的变量与脚本中的成员变量**直接关联**，修改Inspector中的值会实时改变脚本的成员变量值。

---

## 二、变量可见性规则

### 2.1 基本可见性规则

| 变量修饰符 | Inspector中是否显示 | 说明 |
|------------|---------------------|------|
| `public` | ✅ 显示 | 默认可见，可直接编辑 |
| `private` | ❌ 不显示 | 需要额外特性才能显示 |
| `protected` | ❌ 不显示 | 需要额外特性才能显示 |
| `internal` | ❌ 不显示 | 仅在当前程序集可见 |

> ⚠️ **重要提示**：运行中修改的变量值**不会保存**。只有在Unity编辑器中修改的值才会被保存，脚本运行后修改的值不会影响下一次编辑。

---

## 三、控制变量可见性的关键特性

### 3.1 [SerializeField] - 显示私有/受保护变量

**作用**：强制将私有(`private`)和受保护(`protected`)变量在Inspector中显示。

```csharp
public class PlayerController : MonoBehaviour
{
    [SerializeField] private int _health = 100;      // 私有变量，Inspector可见
    [SerializeField] protected float _moveSpeed = 5f; // 受保护变量，Inspector可见
    
    public int maxHealth = 100;                     // 公共变量，Inspector默认可见
}
```

> 💡 **为什么需要**：避免将所有变量设为`public`，保持代码封装性，同时仍允许在Inspector中调整重要参数。

---

### 3.2 [HideInInspector] - 隐藏公共变量

**作用**：使公共(`public`)变量在Inspector中不可见。

```csharp
public class PlayerController : MonoBehaviour
{
    [SerializeField] private int _health = 100;
    
    [HideInInspector] public int maxHealth = 100;     // 公共变量，但Inspector中隐藏
    [SerializeField] public int currentHealth = 100; // 公共变量，Inspector可见
}
```

> 💡 **使用场景**：当变量需要在脚本内部使用，但不需要在Inspector中调整时。

---

### 3.3 [System.Serializable] - 显示自定义类型

**作用**：使自定义类和结构体在Inspector中显示（字典类型无论如何都不能显示）。

```csharp
[System.Serializable] // 必须添加此特性
public class PlayerStats
{
    public string name;
    public int level;
    public float maxHealth;
}

public class PlayerController : MonoBehaviour
{
    [SerializeField] private PlayerStats _playerStats = new PlayerStats();
    
    // 字典类型，无论如何都不会在Inspector中显示
    [SerializeField] private Dictionary<string, int> _inventory = new Dictionary<string, int>();
}
```

> ⚠️ **重要提示**：`Dictionary`（字典）**无论如何都不能**在Inspector中显示，即使添加了`[System.Serializable]`。

---

## 四、辅助特性：增强Inspector体验

### 4.1 分组与说明

| 特性 | 作用 | 示例 |
|------|------|------|
| `[Header("分组说明")]` | 添加分组标题 | `[Header("Player Settings")]` |
| `[Tooltip("说明内容")]` | 鼠标悬停显示说明 | `[Tooltip("玩家最大生命值")] public int maxHealth = 100;` |
| `[Space()]` | 添加字段间隔 | `[Space(10)]` |

```csharp
public class PlayerController : MonoBehaviour
{
    [Header("Movement Settings")]
    [Tooltip("玩家移动速度")]
    [Range(1, 10)] // 设置滑条范围
    public float moveSpeed = 5f;
    
    [Space(10)] // 添加10像素间隔
    [Tooltip("玩家跳跃力")]
    public float jumpForce = 8f;
}
```

---

### 4.2 数值范围与显示控制

| 特性 | 作用 | 示例 |
|------|------|------|
| `[Range(min, max)]` | 限制数值范围 | `[Range(0.5f, 5f)] public float moveSpeed = 2f;` |
| `[Multiline(4)]` | 多行文本显示 | `[Multiline(4)] public string description = "Player description";` |
| `[TextArea(3, 4)]` | 文本区域显示（带滚动条） | `[TextArea(3, 4)] public string notes = "Additional notes here";` |

---

### 4.3 添加自定义操作

| 特性 | 作用 | 示例 |
|------|------|------|
| `[ContextMenu("操作名称")]` | 在Inspector中添加右键菜单项 | `[ContextMenu("Reset Health")] private void ResetHealth() { _currentHealth = _maxHealth; }` |
| `[ContextMenuItem("按钮名", "方法名")]` | 添加按钮，点击执行方法 | `[ContextMenuItem("Reset Health", "ResetHealth")] public int currentHealth = 100;` |

```csharp
public class PlayerController : MonoBehaviour
{
    [SerializeField] private int _currentHealth = 100;
    [SerializeField] private int _maxHealth = 100;
    
    // 添加右键菜单项
    [ContextMenu("Reset Health")]
    private void ResetHealth()
    {
        _currentHealth = _maxHealth;
    }
    
    // 添加按钮
    [ContextMenuItem("Reset Health", "ResetHealth")]
    public int currentHealth = 100;
}
```

---

## 五、Inspector变量操作指南

### 5.1 常见操作

| 操作 | 方法 | 说明 |
|------|------|------|
| **复制脚本值** | 右键点击脚本名 → Copy Component | 复制当前脚本的所有值 |
| **粘贴脚本值** | 右键点击目标脚本 → Paste Component Values | 将复制的值应用到目标脚本 |
| **设置多个对象的值** | 选择多个对象 → 在Inspector中修改值 | 将值同步到所有选中的对象 |
| **重置值** | 点击Inspector中的"Reset"按钮 | 将变量重置为脚本中的初始值 |

> 💡 **小技巧**：选择多个游戏对象时，Inspector会显示它们共有的属性，修改后所有对象的属性会同步更新。

---

### 5.2 多个对象的Inspector操作

1. **选择多个对象**（按住Shift或Ctrl选择多个游戏对象）
2. **Inspector显示共有的属性**
3. **修改值**：所有选中对象的该属性会同时更新
4. **设置特定对象的值**：右键点击属性 → "Set to Value of [对象名]"

![多个对象Inspector操作](https://docs.unity3d.com/uploads/Main/InspectorMultipleObjects.png)

---

## 六、常见问题与解决方案

### 6.1 问题：变量在Inspector中不显示

| 原因 | 解决方案 |
|------|----------|
| 变量是`private`或`protected` | 添加`[SerializeField]`特性 |
| 变量是`public`但想隐藏 | 添加`[HideInInspector]`特性 |
| 变量是自定义类但未添加`[System.Serializable]` | 为自定义类添加`[System.Serializable]` |
| 变量是字典类型 | 无法在Inspector中显示，改用其他数据结构 |

---

### 6.2 问题：修改Inspector值后游戏不生效

| 原因 | 解决方案 |
|------|----------|
| 未在脚本中正确使用变量 | 确保在脚本逻辑中使用了该变量 |
| 变量是`public`但未在Inspector中设置 | 在Inspector中设置值 |
| 运行中修改的值未保存 | 仅在编辑器中修改的值会被保存，运行时修改不会保存 |

---

### 6.3 问题：自定义类在Inspector中显示不正确

| 原因 | 解决方案 |
|------|----------|
| 自定义类未添加`[System.Serializable]` | 为自定义类添加`[System.Serializable]`特性 |
| 自定义类包含非序列化类型 | 确保自定义类中的所有字段都是可序列化的（如`int`、`float`、`string`等） |

---

## 七、最佳实践与示例

### 7.1 脚本规范示例

```csharp
using UnityEngine;

// 必须添加System.Serializable才能使自定义类型在Inspector中显示
[System.Serializable]
public class PlayerStats
{
    public string playerName;
    public int level;
    public float maxHealth;
}

public class PlayerController : MonoBehaviour
{
    // 公共变量，Inspector默认可见
    public string playerName = "Player";
    
    // 私有变量，通过SerializeField显示在Inspector中
    [SerializeField] private PlayerStats _playerStats = new PlayerStats();
    
    // 受保护变量，通过SerializeField显示在Inspector中
    [SerializeField] protected float _moveSpeed = 5f;
    
    // 隐藏的公共变量，仅在脚本内部使用
    [HideInInspector] public int currentHealth;
    
    // 添加说明和范围
    [Tooltip("玩家移动速度，范围1-10")]
    [Range(1f, 10f)]
    public float moveSpeed = 5f;
    
    // 添加右键菜单
    [ContextMenu("Reset Health")]
    private void ResetHealth()
    {
        currentHealth = _playerStats.maxHealth;
    }
}
```

---

### 7.2 Inspector界面示例

![Inspector窗口示例](https://docs.unity3d.com/uploads/Main/InspectorExample.png)

> **说明**：上图展示了包含以下内容的Inspector：
> - 分组标题（"Player Settings"）
> - 带说明的字段（"Player Name"）
> - 带范围限制的字段（"Move Speed"）
> - 带自定义类的嵌套字段（"Player Stats"）
> - 右键菜单项（"Reset Health"）

---

## 八、总结：Inspector变量黄金法则

1. **`public`变量默认显示在Inspector中**
2. **`private`/`protected`变量需要`[SerializeField]`才能显示**
3. **`public`变量可以使用`[HideInInspector]`隐藏**
4. **自定义类/结构体需要`[System.Serializable]`才能显示**
5. **字典类型无论如何都不能在Inspector中显示**
6. **使用辅助特性提升Inspector体验（`[Header]`、`[Tooltip]`、`[Range]`等）**
7. **运行时修改的值不会保存，仅编辑器中修改的值会被保存**

> ✨ **终极提示**：  
> "Inspector窗口是Unity开发中的'控制台'，合理利用可编辑变量，能让你的开发效率提升300%！  
> 但记住：**过度暴露变量会破坏代码封装性**，保持适度是关键。"

---

**附：Inspector变量特性速查表**

| 特性 | 作用 | 适用变量类型 | 重要性 |
|------|------|--------------|--------|
| `[SerializeField]` | 显示私有/受保护变量 | 所有类型 | ⭐⭐⭐⭐⭐ |
| `[HideInInspector]` | 隐藏公共变量 | 所有类型 | ⭐⭐⭐ |
| `[System.Serializable]` | 显示自定义类型 | 自定义类/结构体 | ⭐⭐⭐⭐ |
| `[Header("说明")]` | 添加分组标题 | 任意 | ⭐⭐⭐ |
| `[Tooltip("说明")]` | 添加悬停说明 | 任意 | ⭐⭐⭐ |
| `[Range(min, max)]` | 限制数值范围 | 数值类型 | ⭐⭐⭐⭐ |
| `[Multiline(lines)]` | 多行文本显示 | 字符串 | ⭐⭐ |
| `[TextArea(min, max)]` | 文本区域显示 | 字符串 | ⭐⭐ |
| `[ContextMenu("操作")]` | 添加右键菜单 | 任意 | ⭐⭐⭐ |

> 本指南适用于Unity 2020-2023版本，适用于所有2D/3D项目开发。  
> **记住：掌握Inspector变量，是Unity开发进阶的第一步！** 🚀